apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-log
  namespace: devops
spec:
  storageClassName: manual
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi