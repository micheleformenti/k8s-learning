apiVersion: v1
kind: Service
metadata:
  name: jekyll-node-service
  namespace: development
spec:
  type: NodePort
  selector:
    run: jekyll
  ports:
    - protocol: TCP
      port: 4000
      targetPort: 4000
      nodePort: 30097